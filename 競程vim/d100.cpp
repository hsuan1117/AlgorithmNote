#include<bits/stdc++.h>
using namespace std;

signed main(){
	int T;cin >> T;
	while(T--){
		vector<int> MM[100];
		int n,m;cin >> n >> m;
		for(int i=0;i<m;i++){
			int a,b;cin >> a >> b;
			MM[i].pb(x);
		}		
	}
}
